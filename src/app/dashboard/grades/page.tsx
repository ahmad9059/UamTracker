import { GradesReference } from "@/components/grades-reference";

export default function DashboardGradesPage() {
  return <GradesReference calculatorHref="/dashboard/calculator" showBadge={false} />;
}
